# ── Secrets ───────────────────────────────────────────────────────────────────
.env
serviceAccountKey.json

# ── Large data (not needed at runtime) ───────────────────────────────────────
# 453 MB scraped HTML — rebuild with build_all.py if needed
data/raw_html/
# 236 MB local vector store — replaced by Pinecone
data/chroma/

# ── Build / log artifacts ─────────────────────────────────────────────────────
data/build_errors.jsonl
data/build_report.json
data/scrape_pilot.log
*.log

# ── Eval checkpoints (transient; .md reports are kept) ───────────────────────
data/evals/*.jsonl

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.pytest_cache/

# ── Virtual envs ──────────────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
